
# 원본 백업 (마스킹 안 된 자료) — public push 금지
원본 백업/
**/원본 백업/
_백업/
**/_백업/

# 세션 메타 (로드맵·세션이력 등 클로드 작업 컨텍스트, 외부 공개 X)
_세션/

# 로컬 claude 설정 (개인 권한 등)
.claude/settings.local.json

# 확장 패키징 산출물 (ZIP은 git 포함 금지 — 외부 호스팅 사용)
*.zip
*.crx

# 조사·디버그용 임시 SERP 덤프 (OneDrive가 되살려도 git 제외)
pc_serp_*.html
Program.lnk
.gstack/

# 의존성·빌드 산출물 (Cloudflare/Nuxt가 자동 생성 — git 포함 금지)
node_modules/
**/node_modules/
.output/
.nuxt/
dist/
dist-dry/

# 내부 기획·조직 문서 (제품 기획서·조직 개편안 등 — public repo 노출 금지)
analyzer/plan/
