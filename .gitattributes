# 모든 텍스트 파일은 저장소에 LF(줄바꿈)로 정규화한다.
# 윈도우 에디터/OneDrive가 CRLF로 바꿔도 git이 LF로 통일 → 가짜 변경(줄바꿈 diff) 차단.
* text=auto eol=lf

# 바이너리는 건드리지 않음
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
*.zip binary
